
<div class="container">
  <div class="dialogbox">
    <div class="body">
      <span class="tip tip-left"></span>
      <div class="message">

<span>
	<strong><%= note.title %></strong>
	(<%= time_ago_in_words note.created_at %> ago)
	<br>
	<br>
	<%= note.note %>
  <br>
  <strong> - <%= Reviewer.find(note.reviewer_id).name %></strong>
  <br>
</span> 
<br>

<% if session[:reviewer_id] == note.reviewer_id %>
<%= link_to "Delete note" , [@book,note] , method: :delete %>
<% end %>
    </div>
  </div>